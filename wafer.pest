WHITESPACE = _{ " " | "\t" | "\r" | "\n "}

main = { SOI ~ expression ~ EOI }

operation = { "+" | "-" | "*" | "/" }
number = @{ ASCII_DIGIT+ }

expression = { primary_expression ~ (operation ~ primary_expression)* }
primary_expression = _{ ("(" ~ expression ~ ")") | number }
